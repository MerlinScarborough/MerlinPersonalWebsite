# ============================================
# Windows 本地开发环境配置
# ============================================
#
# 使用方法：
#   1. 复制此文件为 .env:  copy .env.windows .env
#   2. 运行: docker compose up -d
#
# 注意：请根据你的实际路径修改 PROJECT_PATH
#

# 项目根目录的绝对路径（Windows 格式）
PROJECT_PATH=/d/开发（git）/MPW/MerlinPersonalWebsite
